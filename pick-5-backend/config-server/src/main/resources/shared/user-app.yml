spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      
routes:
  base:
    version:
      "/api/v1"
  save:
    "/user/save"
  get:
    "/user/get/{id}"
  get_all:
    "/user/find_all"
  update:
    "/update/{id}"
  delete:
     "/delete/{id}" 

  



